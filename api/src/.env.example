MONGO_URI=mongodb+srv://DIP:<db_password>@dip.omgmmbt.mongodb.net/?appName=DIP
PORT=3000

# AWS S3 — required for Telegram photo uploads (omit to use base64 fallback in dev)
AWS_REGION=ap-southeast-1
AWS_S3_BUCKET=your-s3-bucket-name
# AWS credentials are picked up from environment / IAM role / ~/.aws/credentials
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...